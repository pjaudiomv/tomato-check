<?php
static $smtp_to_name = 'Tomato Slack';
static $smtp_to_address = 'toemail@rsc.org';
static $smtp_email_subject = 'Tomato Import Failed';

static $smtp_host = 'smtp.gmail.com';             // the smtp server
static $smtp_username = 'example@gmail.com';         // the smtp username
static $smtp_password = '';         // the smtp password
static $smtp_secure = 'tls';           // either ssl (port 486) or more securely tls (port 587)
static $smtp_from_address = 'example@gmail.com';     // the address where the email will be sent from
static $smtp_from_name = 'RSC Admin';        // the label name on the from address
